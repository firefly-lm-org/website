# 萤火虫大模型风险管理

**版本**：1.0
**生效日期**：2026-07-25
**维护者**：Core 组（C1/C2/C3）
**审核周期**：每季度首月更新

---

## 原则

本文档列出萤火虫大模型项目当前已知的最高优先级风险，以及对应的缓解措施与触发条件。它不是"问题清单"，而是"当问题发生时我们知道怎么办"的预案。

风险等级定义：
- 🔴 **致命**：出现即导致项目停滞或信任不可逆损失
- 🟡 **严重**：出现会显著拖慢进度或降低估值
- 🟢 **可控**：出现有 workaround，不影响主线

---

## Top 5 技术风险

### Risk-1 🔴：真实 QLoRA 训练路径从未端到端跑通

**描述**
v0.1 全部训练流程使用 `mock_trainer.py` 模拟，产出的是假 loss、假权重 bin 文件。一旦替换为真实 Unsloth/PEFT 调用，存在以下未知：
- CUDA / bitsandbytes / torch 版本兼容性矩阵未验证
- 用户 6GB 显存显卡跑 7B QLoRA 会 OOM，无自动降级到 1.5B 逻辑
- 训练中途断连（合盖/断网/断电）后任务状态机可能卡死在 "running"
- 真实 LoRA safetensors 文件 shape 不匹配导致 FedAvg 聚合崩溃

**影响**
v0.2 核心里程碑（"第一份真实社区权重"）无法交付，估值锚锁死在 v0.1 档。

**缓解措施**
- [ ] v0.2 第一周不写新功能，专人（C1 或发起人）手动跑通"单机真实训练→回传→聚合→推理验证"全流程
- [ ] 记录每一步报错与修复到 `docs/TRAINING_RUNBOOK.md`
- [ ] `requirements.txt` 锁定 torch + CUDA + bitsandbytes + unsloth 版本组合
- [ ] 客户端加 `--max-vram 0.8` 参数，自动检测可用显存并选底座（7B / 1.5B / CPU fallback）
- [ ] 心跳超时阈值写进配置（默认 120s），超时后任务自动回退 `pending` 重发

**触发条件**：v0.2 启动后 5 个工作日内必须跑通至少 1 次真实训练，否则冻结新功能开发，全力排障。

---

### Risk-2 🔴：Non-IID 数据下 FedAvg 聚合退化

**描述**
学术界已知问题：FedAvg 在 Non-IID（非独立同分布）数据上会发散或抵消。萤火虫节点数据天然 Non-IID（有人跑法律、有人跑代码、有人跑医疗），若不处理：
- 多域 LoRA 简单平均 → 模型"什么都学了一点，什么都不精"
- 聚合后权重在某些域的 CEval 子项可能低于底座本身

**影响**
v0.5 "聚合 LoRA 真能改 7B 行为"的验证失败，估值从 ¥800~1200 万砍到 ¥300~500 万。

**缓解措施**
- [ ] v0.2 先在单域（general）用同质数据验证 FedAvg 不退化
- [ ] v0.5 前实现分域聚类聚合（按 `aggregation_key` 分组），不同域独立聚
- [ ] 引入 sqrt(n) 加权（n=该节点样本数），减轻小数据节点噪声
- [ ] 聚合前用 Critic 模型算 PPL 偏离度，异常权重不进聚合
- [ ] 保留"不聚合、只分发个体 LoRA"的降级模式，作为最坏情况兜底

**触发条件**：v0.5 发布前必须有 ≥1 份独立第三方评测（高校/外部开发者）证明聚合 LoRA 在目标域超底座 ≥3 点，否则不宣布 v0.5 可用。

---

### Risk-3 🟡：节点网络信任与防作弊体系未实战检验

**描述**
三级校验（L1 格式/L2 行为/L3 语义）目前只在 ADR 文档里，没有在真实多节点环境跑过：
- L2 校验阈值（训练时长/显存/样本数一致性）是拍脑袋定的，真实分布未知
- 信誉分系统未上线，作弊节点无法被识别和隔离
- 没有"节点身份绑定"机制（同一人开 100 个节点刷贡献值）

**影响**
v0.5 公测时若出现刷分/作弊泛滥，贡献值体系信用破产，企业不敢用。

**缓解措施**
- [ ] v0.2 起在测试环境模拟 ≥3 种作弊模式（伪造 SHA256、压缩时间、复制他人权重），验证 L1/L2 能否拦截
- [ ] v0.5 前实现节点硬件指纹（GPU UUID + CPU info hash），同一指纹只计 1 个贡献值
- [ ] 信誉分初版：初始 100，作弊扣 30，三次清零 + IP 封禁
- [ ] 贡献值分布定期公示（Gini 系数），异常集中触发人工审查

**触发条件**：v0.5 公测前 L2 校验必须在 ≥10 节点测试环境跑通 72 小时无漏检。

---

### Risk-4 🟡：非技术创始人的 Bus Factor=1

**描述**
项目核心架构判断、治理决策、外部沟通高度集中在发起人（徐喆）一人：
- 发起人失联/生病/放弃 → 项目停摆
- 所有 CLA/DCO 签署、商标申请、商业合同都依赖一人操作
- AI 辅助编码产出的代码，发起人能"用"但不一定能"审"

**影响**
投资人尽调时 Bus Factor=1 直接扣 20~30% 估值；开放原子引入期 TOC 会质疑项目可持续性。

**缓解措施**
- [ ] v0.2 前确定 C2（训练侧）候选人名单并发出邀请
- [ ] v0.5 前 Core 组至少 3 人（C1/C2/C3）全部签 ICLA 并拥有 ADR 签字权
- [ ] 关键操作（商标续费、域名续费、云服务器续费）设日历提醒 + 备用联系人
- [ ] 所有架构决策必须有 ADR 文档，不依赖个人记忆
- [ ] 发起人每周投入时间记录在 `docs/FINANCE.md` 或单独 `HOURS.md`，让外人看到项目不只靠一个人

**触发条件**：v0.5 发布时 Core 组不足 3 人 → 推迟发布，先补人。

---

### Risk-5 🟢：依赖单一底座（Qwen3）的生态锁定

**描述**
当前架构绑定 Qwen3  tokenizer / chat template / 模型格式：
- 若 Qwen3 后续版本不兼容（如 tokenizer 变更），所有社区 LoRA 需重训
- 若阿里云对 Qwen3 开源协议做限制性修改（虽然概率低），项目需紧急切换底座
- 社区认知"萤火虫=Qwen3 微调工具"，限制扩展到 Llama/Mistral 的叙事

**影响**
中长期架构灵活性受限，但不是短期致命问题。

**缓解措施**
- [ ] v0.2 的 `config.yaml` 设计时就抽象出 `base_model` 字段，支持多底座声明
- [ ] v1.0 前验证至少 1 个备选底座（如 Llama-3.1-8B 或 InternLM3）能跑通同一套 LoRA 流程
- [ ] 聚合协议不绑定特定 tokenizer，按 token id 空间分桶
- [ ] 文档中明确"萤火虫是底座无关的聚合协议，Qwen3 是当前默认底座"

**触发条件**：持续监控，不 blocking 任何版本。

---

## 运营风险（简表）

| 风险 | 等级 | 缓解 |
|------|------|------|
| 商标被抢注 | 🟡 | v0.2 内完成 CNIPA/USPTO 搜索并提交申请 |
| 域名到期忘记续费 | 🟢 | 设自动续费 + 备用联系人 |
| 爱发电/捐赠中断 | 🟢 | 个人储蓄兜底 3 个月运维 |
| 云厂商赞助撤回 | 🟢 | 多赞助方分散，不依赖单一来源 |
| 核心贡献者离职 | 🟡 | 90 天静默规则 + 候补名单 |
| 负面舆论（"标题党"质疑） | 🟡 | 永远不夸大，所有数据可复现可审计 |

---

## 附录：风险监控仪表

每个风险的状态更新记录在下方，每月 review：

| Risk ID | 名称 | 当前等级 | 状态 | 最后更新 |
|---------|------|---------|------|---------|
| Risk-1 | 真实训练路径 | 🔴 | 未触发（v0.2 待验证） | 2026-07-25 |
| Risk-2 | FedAvg Non-IID 退化 | 🔴 | 未触发（v0.5 待验证） | 2026-07-25 |
| Risk-3 | 节点信任体系 | 🟡 | L1 已实现，L2/L3 待实战 | 2026-07-25 |
| Risk-4 | Bus Factor=1 | 🟡 | 缓解中（C2 候选人接触中） | 2026-07-25 |
| Risk-5 | 底座锁定 | 🟢 | 持续监控 | 2026-07-25 |

---

> 本文档本身是风险缓解措施的一部分：它证明项目承认风险、有预案、可审计。
